{
    "service": {
        "name": "{{ container_name }}",
        "tags": ["service"],
        "port": 80,
        "address": "{{ ip }}",
        "checks": [{
            "id": "api",
            "name": "HTTP on port 80",
            "http": "http://{{ ip }}{{ http_address }}",
            "interval": "10s"
        }]
    }
}